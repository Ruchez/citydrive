import SellSection from '../components/SellSection'

const Sell = () => {
    return (
        <main className="page-transition">
            <div className="reveal revealed" style={{ paddingTop: '80px' }}>
                <SellSection />
            </div>
        </main>
    )
}

export default Sell
